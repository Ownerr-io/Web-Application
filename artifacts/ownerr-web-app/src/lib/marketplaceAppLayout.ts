import {
  AUTH_APP_HEADER_INNER,
  AUTH_APP_MAIN_PADDING,
  AUTH_APP_PAGE_STACK,
} from "@/lib/appDeskLayout";

/** Main-column layout for authenticated marketplace desk pages (matches OWNERR OS app). */
export const MARKETPLACE_APP_CONTENT_CLASS = AUTH_APP_PAGE_STACK;

export const MARKETPLACE_APP_MAIN_PADDING_CLASS = AUTH_APP_MAIN_PADDING;

export const MARKETPLACE_APP_HEADER_INNER_CLASS = AUTH_APP_HEADER_INNER;
