import {
  AUTH_APP_MAIN_PADDING,
  AUTH_APP_PAGE_STACK,
} from "@/lib/appDeskLayout";

/** Main-column width for authenticated OWNERR OS app pages (95% of area beside sidebar). */
export const OWNERR_OS_APP_CONTENT_CLASS = AUTH_APP_PAGE_STACK;

/** Padding on DashboardLayout main so content spans ~95% of the main column. */
export const OWNERR_OS_APP_MAIN_PADDING_CLASS = AUTH_APP_MAIN_PADDING;
